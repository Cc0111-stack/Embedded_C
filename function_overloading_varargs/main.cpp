// 使用 C 语言的可变参数（Variadic Arguments）模拟"函数重载"
// 通过可变参数实现类似函数重载的功能

#include <stdio.h>
#include <stdarg.h>

// 接受可变数量参数的函数
static void va_func(int n, ...)
{
    va_list ap; // 声明可变参数列表变量

    va_start(ap, n); // 初始化 ap，使其指向第一个可变参数

    // 循环取出所有可变参数
    while (n--)
        printf("%d \n", va_arg(ap, int));

    va_end(ap); // 清理工作
}

// 主函数 - 演示可变参数实现的函数重载效果
int main()
{
    printf("--- va_func(1, 100) ---\n");
    va_func(1, 100); // 传入 1 个参数

    printf("--- va_func(2, 200, 300) ---\n");
    va_func(2, 200, 300); // 传入 2 个参数

    return 0;
}

/**
 * ============================================================
 *  可变参数 vs 函数重载：C vs C++
 * ============================================================
 *
 * ┌──────────────────────┬──────────────────────┬───────────────────────────┐
 * │       对比项          │    C 可变参数         │   C++ 函数重载             │
 * ├──────────────────────┼──────────────────────┼───────────────────────────┤
 * │ 实现机制              │ va_list / va_arg 宏   │ 编译器根据参数类型/        │
 * │                      │ 运行时遍历参数列表     │ 数量选择匹配的函数         │
 * ├──────────────────────┼──────────────────────┼───────────────────────────┤
 * │ 参数解析时机          │ 运行时（runtime）      │ 编译期（compile-time）     │
 * ├──────────────────────┼──────────────────────┼───────────────────────────┤
 * │ 参数数量              │ 可变（运行时指定）      │ 固定（每版本重载参数数固定） │
 * ├──────────────────────┼──────────────────────┼───────────────────────────┤
 * │ 类型安全              │ 弱（无类型检查）       │ 强（编绎器检查参数类型）    │
 * ├──────────────────────┼──────────────────────┼───────────────────────────┤
 * │ 调用语法              │ 同一函数名 + 不同参数   │ 多个同名函数，编译器       │
 * │                      │ 数量，需告知参数个数    │ 自动匹配最佳版本            │
 * ├──────────────────────┼──────────────────────┼───────────────────────────┤
 * │ 典型应用              │ printf/scanf/日志系统  │ 操作符重载、构造函数重载等  │
 * └──────────────────────┴──────────────────────┴───────────────────────────┘
 *
 *  总结：
 *    C 的可变参数是一种"轻量级"的重载模拟，
 *    适合参数类型相同但数量不确定的场景。
 *    缺点是失去了类型安全检查，需要调用者确保传入正确的参数。
 *
 *    在嵌入式开发中，可变参数常用于：
 *      - 日志打印系统
 *      - 断言宏（assert）
 *      - 格式化输出函数
 * ============================================================
 */